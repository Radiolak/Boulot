# A ajouter dans CHACUN des deux jobs suivants (node_exporter et process_exporter),
# juste apres la ligne "job_name:", au meme niveau que "static_configs:"

  relabel_configs:
    - source_labels: [__address__]
      regex: '([^:]+):.*'
      target_label: host
      replacement: '${1}'

# Exemple complet pour node_exporter (meme bloc a dupliquer sur process_exporter) :
#
# - job_name: 'node_exporter'
#   relabel_configs:
#     - source_labels: [__address__]
#       regex: '([^:]+):.*'
#       target_label: host
#       replacement: '${1}'
#   static_configs:
#     - targets:
#       - 'chara.fr.airbusds.corp:9100'
#       - 'coxa.fr.airbusds.corp:9100'
#       ... (le reste de ta liste inchangee

sudo promtool check config /etc/prometheus/prometheus.yml
sudo systemctl reload prometheus


curl -s 'http://localhost:9090/api/v1/query?query=node_uname_info' | grep -o '"host":"[^"]*"' | head -5

